
# 2026-08-24 — Live resupply test (triggered from watch)

Triggered a live resupply while the duo was in the deep sewer (added wait.watch --needs_resupply--> resupply so a due-while-idle duo goes to resupply; a keeper feature).

## Bug the live test exposed (user's #1 concern, real)
Supporter got STRANDED: leader recalled to 중앙 광장 and waited; supporter stayed in 갈라진 틈 (sent 귀환 once, then just polled). Root cause: 귀환 is a TELEPORT — it doesn't carry the follower AND emits no "left" line, so the supporter's `together` flag stayed stale-True → it never re-followed or retried its own recall.

## Fix (behavior-only, hot-reloaded live)
- do_recall now sets s.together=False (teleport → co-location unknown until re-seen).
- Supporter branch restructured: sup_recall (do_recall + retry_recall until `at_anchor` = room is 중앙 광장 or partner seen) → sup_follow (form_party + tag_along). So the supporter recalls-with-retry to the anchor, THEN follows.
- Verified LIVE: after hot-reload the stranded supporter recalled to 중앙 광장 and started 따라; leader now seesPartner=True. Reunited.

## MV gate working (user's gap fix, confirmed live)
Leader held at recall_regroup (봐) because ready_to_walk needs BOTH mv>=min_mv(100); supporter mv was 42 (drained by the stranding+retries), recovering 42→70→... standing. Gate correctly prevents walking until recovered (cap stuck:180).
Observation: standing MV recovery is slow (~0.5/s); min_mv=100 is conservative (round-trip ~30-40 moves). Options if the anchor wait is annoying: lower min_mv, or 쉬어 (sit) in recall_regroup for faster MV regen. Leaving as-is for now.

Tests: test_hunt_cycle.test_duo_resupply_roles updated (sup_recall → sup_follow); all 6 suites green.

## Item-attack spam fix + watch roams for regen (user, behavior-only)
- SPAM BUG (critical; likely caused the earlier disconnect): hostiles() classified ground item 훈련생바지 as a mob → attacked every tick (no engage → not fighting → re-attack). FIX (hunt_step, behavior): _pick_target skips ground items (놓여/떨어져 markers) — attacks real mobs only; plus anti-spam: if we just attacked a target and it didn't engage (not in_battle within 3s), re-look (봐) instead of re-attacking. Verified live: now attacks 뱀/지네 (real), 봐 between, no 훈련생바지, no spam.
- REGEN CHECK now ROAMS: watch_poll (leader-only) every [hunt].regen_check MOVES to another zone room (hunt_nav coverage) + 봐 — respawns may not be in the exact room. Supporter follows.
- min_mv lowered 100 -> 40 (100 caused multi-min anchor stalls after MV drain).
- Recovered the overnight run: it had DISCONNECTED at 00:07:37 (no auto-reconnect, overnight off) during the resupply churn my repeated edits caused. Reconnected both, and after the user moved them back to 갈라진 틈 + stopped the spam, re-enabled engine->hunting in place. Now hunting real mobs, together, supporter casting 방비. All 6 suites green.
- LESSON: hot-reload re-enters the current workflow from top; editing while a RESUPPLY is mid-flight re-runs do_recall (귀환) and drains MV — avoid editing during resupply. Editing during hunting/wait is safe (no recall on entry).

## Overnight outcome: parked safely (unattended run not yet reliable)
After re-enabling, the duo SEPARATED again on the resupply go_home route (leader → sewer, supporter stranded at 중앙 광장 spamming 따라; resume_hunt has no cap → would deadlock). Reunited via 귀환. Retried hunt_start with town-resupply disabled (maintenance_interval=999999), but it got STUCK in travel's `reform` at the anchor — reform has step=noop (no 봐), so no fresh RoomSeen → `together` never set → party_ready never fires. Couldn't hot-fix safely (engine on → reload re-enters → re-fires 귀환 → churns/drains MV).
Decision: stopped gambling; PARKED both safely via 끝 (reunited at 중앙 광장, location saved, logged out). Monitor loop stopped.

### Fixed & validated tonight
- Item-attack spam (훈련생바지) — _pick_target skips 놓여/떨어져 items + anti-spam re-look. Live-confirmed (hunted 뱀/지네 cleanly).
- Fountain-drink-to-full (hunger), MV gate, recall-stranding fix, wake-on-both-recovered, watch-roam. Resupply cycle DID complete once end-to-end.

### REMAINING BUGS (for the morning, fix while engine OFF to avoid re-enter churn)
1. reform (travel) and resume_hunt need step="look" (봐) to refresh co-location — else party_ready deadlocks. (recall_regroup already does this; copy it.)
2. resume_hunt has NO stuck cap → add one (fallback like recall_regroup's stuck:N).
3. Route-follow separation: supporter falls behind on long multi-room routes (go_home/travel) incl. manholes; needs the leader to wait for the follower per-step, or a mid-route re-sync.
4. No auto-reconnect for engine runs (duo keeper's reconnect is gated on duo.overnight). A disconnect → dead until manual reconnect.
5. KEY OPERATIONAL RULE: never edit behavior files while a workflow that RECALLS (resupply/travel) is mid-flight — hot-reload re-enters from top and re-fires 귀환.

## Fixes deployed + LIVE-VALIDATED (2026-08-24 daytime)
Rebuilt the hunt coverage/exhaustion and the duo route-follow; restarted the server and ran a live hunt.

### Changes
- **Premature-barren FIX (the new bug).** `area_barren` no longer uses a blind move count. New `coverage.next_move`/`swept` do a frontier-first sweep confined to the **same-title connected component** around the hunt anchor (not the map "zone" — the 9-room 갈라진 틈 pocket is lumped into a 225-room zone, and 122 rooms share that title but only the local 12 are one connected component). Exhaustion fires ONLY when every room in the area is visited and every in-area exit walked. Uses `neighbors_all` (reverse-inferred edges count as walked) and the session `blocked_exits` (never re-issues a CantGo/phantom exit). Safety ceiling `sweep_cap=40` mob-less moves. `hunt_anchor` captured on first sweep step, reset per hunt in `engine.boot`.
- **reform/arrive/resume-hunt deadlock FIX.** Those states now `봐` (step="look") to refresh co-location so `party_ready` can fire; added stuck-caps (reform→to_anchor re-recall; run_route→reform; resume_hunt→hunting) so the group can never permanently deadlock.
- **route-follow separation FIX.** `route_step` now paces to the follower: the leader only takes the next route step once the partner is back in-room (else `봐` and wait); run_route/go_home stuck-caps recall+reunite if the follower is truly lost.
- **Engine auto-reconnect.** On a socket drop during an engine hunt, the session reconnects (throttled 30/120/120s) and re-arms via travel (recall→route→hunt) — previously only the duo `overnight` keeper reconnected.

### Live run evidence (target 한성의 하수구, recorded route)
Route ran cleanly (귀환→모두 그룹→ㅂㅅㅅ 훈열 ㅅ ㄴㄴㄴ 맨열 아래). Dozens of 벌레/거미/훈련생 kills across many rooms, every 공격 followed by 봐 — no spam, no item-attack. Supporter followed + 자동지원 + 방비 걸어 the whole time — zero separation. Rested (자/깨/일) only after clearing, then watch/점수 poll for regen. ALL green.

### Tuning note (not a correctness bug)
The sweep confines to the arrival area's same-title component. If a route drops the duo where the local component is tiny (this run ended in the 2-room 개미굴 dead-end after sweeping north), they clear it fast and wait for regen there rather than roaming back to richer rooms. The user's real target pocket (갈라진 틈, 12+ connected rooms) is well-formed. Future: a `hunt_radius`/richer roam, or record a route straight to 갈라진 틈.

## Steered to the 갈라진 틈 pocket (deep route) — SUCCESS
The pocket is a map fragment (manhole 아래 drops break dead-reckoning → disconnected graph; no auto-path). But it's the endpoint of the recorded **"한성의 하수구 바닥"** route (base route + ㅂㅂㅅㅅㄴ 아래 ㅅㅂ 아래 — three manhole drops). `hunt_start target="한성의 하수구 바닥"` ran the full route cleanly and landed BOTH in 갈라진 틈, together. Coverage swept the whole pocket (repeating 북동남남서북북 pattern = systematic), found it momentarily empty of mobs, rested, now watch/점수 for regen. Confinement (same-title component) works perfectly here: 갈라진 틈 = a clean 12-room connected component.
- Note: first sweep did ~40+ moves (hit sweep_cap) because the pocket has unexplored/phantom exits (위 etc.) the frontier keeps probing; those get added to blocked_exits on CantGo, so later sweeps converge faster. Drained supporter MV to ~19; the rest cycle recovers it. Self-correcting.
- LESSON for reaching deep pockets: use the deepest matching recorded route as the hunt_start target — the route command sequence reaches fragmented areas the graph can't path to.

## RESTORED 지도-driven navigation (was dead code the engine bypassed) — LIVE SUCCESS
User's catch: we'd decided to drop title/dead-reckoning and identify rooms via 지도. But `find_by_fingerprint` had ZERO callers, the engine hunt never sent 지도 (only the old walker did), and `_position_of` minted a new node on every unrecorded move — so a 9-room pocket of identical "갈라진 틈" rooms became a 166-room phantom snake, and the sweep only orbited columns 2-3.

Fix — new `gumiho/gridsweep.py`: a 지도-driven local sweep. Room identity = the cell's POSITION in the server's own 지도 minimap (parsed by mapgrid.py), never the title. One 지도 reveals a whole neighbourhood (cells + drawn connections); we fold it into a relative grid (anchored so `here` == our position), walk to the nearest unvisited cell, and know we're done only when every cell is visited. Wiring: webui folds each MapGrid into `ctx.flow["gridsweep"]`; `hunt_step` asks for 지도 on an unknown cell then walks the grid; `area_barren` = grid swept; kept in `ctx.flow` so it resets per hunt-entry and persists across inner states. Also fixed travel `to_anchor` to recognise 중앙 광장 by `at_anchor` (landmark title) instead of the map-fragile `recalled` (graph route to fountain), which was failing right after recall on the polluted map.

Live (target 한성의 하수구 바닥): recalled → deep route → pocket → `지도 지도` → swept ALL directions incl. 서/column-1 (`남 동 북 북 서 서 남 남 남`) → 30+ 뱀/지네 kills, no spam/no items → rested when cleared → watch/regen. Only 2 지도 needed (one view mapped the neighbourhood). All 7 test suites green (+ new test_gridsweep: 3x3 from center & corner, 2x5 corridor all fully swept).

### Still open (separate, non-blocking): the PERSISTENT map still mints phantom rooms
The hunt no longer relies on the persistent graph, but `_position_of` still dead-reckons and mints clones for identical rooms (166 phantom 갈라진 틈 rooms accumulated). Next: give the persistent map the same 지도-fingerprint identity (wire find_by_fingerprint into _position_of), and purge the existing phantoms. Town nav (resupply's `recalled`) shares the map-fragility risk.

## Zone-map overlay + per-room avoid control (user's navigation vision) — BUILT & LIVE-VALIDATED
Vision: ignore the persistent map/graph nav entirely. Town/site moves = predefined recorded routes; in-zone = recognized on the fly (the 지도 gridsweep), treated as a TEMPORARY per-hunt map. Show it as a translucent UI overlay; user checkboxes mark rooms to avoid hunting in (runtime state, not files).

Built:
- `GridSweep` (gumiho/gridsweep.py) now carries `avoid` (runtime set), `version`, `snapshot()`; `next_dir` skips avoided cells as hunt targets (still traversable); `mark_visited/move/ingest/set_avoid` bump version. Moved the sweep from `ctx.flow` to `ctx.gridsweep` so it PERSISTS across hunt/wait/rest cycles (the learned map + avoid marks survive resting) and resets only on a new hunt (engine.boot).
- webui: `_broadcast_gridmap` pushes the snapshot on every version change (engine loop); ws `grid_avoid` sets/clears a cell's flag at runtime; ws connect pushes the current gridmap so a freshly-opened page shows the overlay immediately.
- Frontend (static/index.html): translucent `.zonemap` overlay per pane; renders cells by relative coord (@ current, ■ swept, □ unswept, red = avoid) with a checkbox per cell → `grid_avoid`.
- hunt_step: passes THROUGH an avoided cell (no fighting) instead of hunting there.
Live-validated: fresh client got a 3×3 grid (9 cells, here=[1,1], 12 edges) built live from 지도 as the duo swept; avoid round-trip confirmed (mark -> echoed avoided; sweep skips it). New test_gridsweep avoid test + all 8 suites green. NOTE: reload the browser tab to load the overlay HTML.

## Fixed: route_step 봐-SPAM (server burden)
The per-step follow-pacing 봐'd every engine tick when the partner wasn't in the room — hammering the server when the duo separated. Now throttled (re-look at most every 3s) with a per-step cap (route_pace_cap=10s) then proceed. Behavior-only (hot-reload).

## STILL OPEN: duo route separation (pre-existing, worked around manually)
travel has a RACE: the leader's reform->run_route passes and it starts walking the recorded route before the supporter's `따라` is established (supporter still in its own reform), so the leader out-runs the follower and strands it (seen: leader at 훈련장 입구, supporter drifted to 천년 수목 아래). Recall points are NOT divergent (both 귀환 -> 중앙 광장). Workaround this session: walked both to the pocket manually at a follow-safe pace (2s/step), then engine_boot hunting in place — swept + killed cleanly, overlay populated. PROPER FIX (next): walk the predefined route in LOCKSTEP — both characters send each step (don't rely on 따라), synced per step — matching the user's "predefined moves" model.

## Fixed: low-HP freeze (rest attempted mid-battle) + overlay only-during-hunts
BUG (user-found): character switched clear_room->rest on low HP, but 자 is refused mid-battle, so it got stuck in rest.sleeping (exits only on full recovery) — neither fighting the mobs in the room nor recovering.
FIX (user's design — a sticky "rest needed" latch):
- `_REST_PENDING[sid]` runtime latch (hunting.py), survives the clear_room->rest workflow switch. Reflex `needs_rest_flag`->`flag_rest` sets it ONCE when hp<30% or mp<20% (fires once because the arbiter runs one reflex then returns — a match-every-tick reflex would starve the transition). clear_room now switches to rest on `rest_ready` = latched AND not in_battle AND partner not in_battle (so we never switch into a 자 the server refuses; we keep fighting while low and rest the moment the battle ends). rest.sleeping gets `in_combat -> hunting` (attacked while asleep -> fight, latch persists, rest again after). waking clears the latch. Behavior-only (hot-reload).
- Validated: rescued the live stuck character via engine_boot hunting; HP recovered 283->474 and it resumed fighting. test_engine rest case updated (latch takes a tick); all 8 suites green.
- Overlay "only during hunts": webui `_broadcast_gridmap` + on-connect push now gated on engine enabled AND workflow in {hunting,wait,rest}; sends gridmap:null (frontend hides) otherwise. webui change — applies on next restart.

## Added: resupply after zone-clear + full recovery
Request: when the zone is all cleared and both are fully recovered, do a resupply (use the downtime; return topped-up).
Impl (behavior-only, hot-reload): new guard `resupply_due` (resupply.py) = bottle_dry OR since_resupply > [resupply].clear_interval (default 120s). Wired into wait.watch AFTER `mobs_present`: cleared -> recover(sleep to full) -> wake -> watch -> [regen? hunt : due? resupply : keep watching]. auto_cycle (set during the hunt) makes the resupply RETURN to hunting. clear_interval throttles so a fast-regen zone isn't a town trip every cycle. New test_resupply_after_clear + all 8 suites green.
CAVEATS (told user):
1. The auto-return (go_home) walks the recorded route for hunt_zone/hunt_target — so the hunt must be started with hunt_start(<destination>) for the return to land back in the right pocket. A session booted via bare engine_boot (no target, e.g. this session's rescue) would return to the DEFAULT zone.
2. The return leg exercises route_step, which still has the unfixed route-follow SEPARATION race — so resupply may separate the duo on the way back until the lockstep-route fix lands. (maintenance_interval stays 999999; only this clear+recover path enables resupply.)

## Fixed: resupply fountain went WEST (graph-nav), drink failed, still hungry
Cause: [routes] had macros for 대장간/치료실 but NOT for the fountain (광장 사거리), and enter_goto_fountain used _setup_goto(prefer_macro=False) -> graph pathfinding on the polluted map -> returned 서. It walked west, never reached the fountain, so the 25 분수 마셔 hit nothing (hunger unresolved). Repairs still worked because that leg's graph-nav happened to succeed.
Fix (behavior-only, hot-reload): added [routes]."광장 사거리" = ["남","남"] (the known town route, matches duo._town_maintenance) and set enter_goto_fountain prefer_macro=True — fountain is now a FIXED route, never graph-pathed. Left the smith on graph-nav (it worked; no stuck-cap on goto_smith so a wrong predefined route could strand it — make it a macro once verified live). All 8 suites green; test_resupply now shows 귀환,모두그룹,남,남,버드 물 채워,분수 마셔... (correct).
Applies on the NEXT resupply (fixes the lingering hunger then). CAVEAT still stands: a session booted without a destination (this session's rescue) returns via the default zone route on go_home — start via hunt_start(<destination>) for a correct auto-return.

## Rescue: character trapped in 기증의 방 (junk shop, west of 중앙 광장) attacking a 검
The OLD fountain graph-nav (서) had sent the resupply into 기증의 방 (a donation/shop room, exits 동 only). It got stuck: the shop's aura items ("백운의 안개가 둘러쌓인 검이 꿈틀거린다 ...뿌연 오로라에 둘러싸여 있습니다") have NO 놓여/떨어져 marker, so _pick_target treated the 검 as a mob and kept attacking it (never moving on).
Rescue: disabled engine, walked both 동 -> 중앙 광장 (reunited, healthy), then hunt_start("한성의 하수구 바닥") -> travelled back to 갈라진 틈 together and resumed hunting. 
Fixes: (1) fountain now a fixed macro (남,남) — already covered above; (2) added "오로라" to _ITEM_MARKERS so aura-wrapped shop display items are skipped. All 8 suites green. Re-established WITH a destination so the resupply return uses the recorded deep route (not graph fallbacks). Monitoring the next resupply cycle to confirm the fountain fix live.

## Fixed: resupply recall halt + 봐-spam + the REAL "still hungry" cause
Cascade of issues found while the user tested the resupply-after-clear:
1. **[routes] header deleted** — my earlier fountain-macro edit dropped the `[routes]` table header, orphaning ALL route macros into [places] (routes went empty). So `recalled` (directions_to fountain) returned None -> the leader recalled once, retried, hit recall_exhausted -> HALTED. FIX: restored the header; routes now has 광장 사거리/대장간/치료실.
2. **Leader recall used fragile `recalled`** (graph route to fountain). Changed resupply recall to `at_anchor` (중앙 광장 title), matching travel + the supporter. Robust.
3. **recall_regroup 봐-SPAM**: step="look" sent 봐 every tick while waiting for a disconnected/absent partner -> server hammer. FIX: throttled the `look` action to once / 3s (covers regroup/reform/recall_regroup/arrive/resume_hunt).
4. **THE REAL "still hungry": wrong drink command.** 광장 사거리 room says '"분수대 마셔" "물 마셔"' — config had "분수 마셔" (no 대), which the server silently ignores. So every past "drink" did NOTHING. FIX: fountain_drink = "물 마셔". (Correct earlier memory: 분수 마셔 was WRONG.)
5. **hungry/thirsty flags never cleared** (latched True forever) — made full characters look hungry. FIX (substrate, applies on restart): parse "배가 부릅니다"(fed) / "배가 불러…"(full: hunger+thirst) / "목이 마르지 않"(quenched) -> clear the flags. drank_enough now stops at 'full' else the count cap.
All 8 suites green. Manually fed both (물 마셔 -> "배가 불러 더이상 마실 수 없습니다" = full) — they're fed + together at 광장 사거리, engine off.
ROOT ISSUE STILL UNSOLVED: duo SEPARATION on town/return travel (supporter fell behind AND disconnected/reconnected this run). Every individual mechanic works; the duo just won't stay together through multi-step travel. Needs the LOCKSTEP-route fix (both send each step in sync) — proposed to user.

## LOCKSTEP TRAVEL (option A) — the definitive duo-separation fix. LIVE SUCCESS.
Old model: leader walks the route, supporter follows via 따라 -> the leader out-paces / the follower loses the trail (esp. at manholes) or drops connection -> separation. Symptom-patched for a week; now fixed properly.
New model: BOTH characters walk the SAME recorded route step-for-step, kept in sync by the director; 따라 is used ONLY for the hunt sweep, never for travel.
- **Parser** (substrate): catch the server's own arrival messages so `together` is set without 봐-polling — "X가 갑자기 나타났습니다" (recall/teleport), "여기 날아왔습니다" (fly-in), "X님이 다시 접속했습니다" (reconnect), "…쪽에서 들어옵니다/왔습니다" (directional). (User's tip: cover the recall case #2.)
- **Director** (substrate): `route_reset/route_ready/route_advanced` (shared per-sid step counter — neither may get >1 step ahead), `both_routes_done` (finish together, so the leader doesn't reach `arrive` a step early and strand the follower), `partner_lockstep` (are we both route-walking?). ctx.director wired via engine.
- **route_step** (behavior): LOCKSTEP when both are route-walking (travel.run_route); else PACE to the partner via the arrival message (resupply go_home, where the supporter 따라-follows). No 봐.
- **travel.toml**: reform -> `group_no_follow` (group for combat, NO 따라 — else auto-follow double-moves and desyncs); arrive -> `form_party` (establish 따라 for the hunt).
- Also this turn: throttled `look` (봐 ≤ once/3s in waiting states); hunger/thirst flags now CLEAR on 배가 부릅니다/불러/목이 마르지 않 (were latched forever); fountain drink command corrected to "물 마셔" (분수 마셔 was silently ignored — the real "still hungry" cause); [routes] header restored; resupply recall uses at_anchor; 오로라 shop-item skip.
New test_lockstep.py (both walk a route incl. a 아래 manhole, never >1 apart; solo still walks). All 9 suites green.
LIVE: hunt_start 한성의 하수구 바닥 -> BOTH walked the full deep route (ㅂㅅㅅ 훈열 ㅅ ㄴㄴㄴ 맨열 아래 ㅂㅂㅅㅅㄴ 아래 ㅅㅂ 아래) in sync, reached 갈라진 틈 TOGETHER, supporter then 따라'd for the sweep, leader hunting. No separation.

## Gear repair — FIXED (inferred fountain→smith route). Full resupply cycle works.
The 대장간 leg walked the anchor-relative macro FROM the fountain and overshot to 종로 사거리 (last 남 hit a wall) -> 수리 fired in the wrong room, no repair. User confirmed: [routes]."대장간" = [남,남,동,동,남] is FROM 중앙 광장, 수리 command is correct.
FIX: enter_goto_smith now INFERS the fountain→smith segment by stripping the fountain macro prefix from the smith macro ([남,남,동,동,남] minus [남,남] = [동,동,남]) — since 중앙광장→대장간 runs through the fountain. The accumulated trail (fountain+segment) = the full anchor→smith path, so walk_back reverses straight home. Robust: derives from the two recorded routes; _strip_prefix falls back to the full route if no common prefix.
LIVE: resupply -> 귀환, 모두 그룹, 남,남 (fountain), 버드 물 채워, 물 마셔, 장비, 동,동,남 (fountain→smith), 은갑옷/청동갑투구/새갑 방패 수리 -> blacksmith actually repaired ("대장장이가...망치로...됐습니다. 수리비 480원") -> gear now 50/50/50 (was 26/42/41). Walk_back + clinic clean. Plus the gear_worn auto-trigger (장비 every 180s; resupply at <=60% durability) will keep it repaired during hunting.
FULL RESUPPLY CYCLE NOW WORKS: recall -> fountain(drink, fixes hunger) -> refill -> inspect -> smith(repair) -> return. All 9 suites green.

## Regen detection — ROOT CAUSE + fix. And a stale-`together` deadlock.
User: "waiting for regen, polls 봐, never gets proper input." ROOT CAUSE: the watch state polled 점수 (watchdog) — which carries vitals but NO room render — so ctx.entities stayed STALE from before the clear; respawned mobs (they spawn silently in place) were never SEEN. Confirmed live: a manual 봐 revealed 거대한 뱀×2 + 커다란 지네×2 present while the bot sat polling 점수, not attacking.
FIXES (behavior, hot-reload):
1. wait.watch: was `watch_poll` (봐 once per 5 min, one room). Now step=noop; after `regen_elapsed` (regen_check=180s) it SWITCHES BACK TO HUNTING and RE-SWEEPS the whole pocket (walking every room + 봐 is what finds silent respawns). +gear_worn/resupply triggers kept.
2. hunt_step hold branch: `if partner and not together: return` used to hold with NO re-look -> `together` goes STALE-False (last render predates the follow) and it deadlocks even when the partner is right here (both at 갈라진 틈, oscillating regroup/clear_room, polling 점수). Now re-looks (봐) at most every 3s while holding to refresh co-location.
Rescue: engine off -> re-boot hunting -> regroup couldn't confirm party_ready in 25s -> lockstep travel back -> arrived together -> now hunting the respawned 거미/벌레. All 9 suites green.
RESIDUAL (flagged): `together` is flaky during the hunt sweep (frequent hold-봐 refreshes; possibly the FLYING supporter's render or follow-lag) — functional but worth root-causing (reduce the 봐 volume). regroup's party_ready was slow to fire at re-boot even with both present.

## Fixed: `together` mis-tracking for the FLYING supporter (parser departure regex)
User corrected me: combat NEVER stops following. Real bug: the supporter is a FLYING char, and it leaves a room with "…쪽으로 날아갔습니다" (flew away) — LEFT_RE only matched "…쪽으로 떠났습니다" (walked). So a flying departure never cleared `together`, and co-location tracking drifted (leader mis-tracks whether the partner is with it -> stale together -> the 점수/모두그룹 deadlock and the 봐-churn). FIX (parser, substrate): LEFT_RE now matches (?:떠났|날아갔)습니다; VANISH_RE also catches "귀환합니다" (partner recalled away). Arrival was already covered (여기 날아왔습니다 / 갑자기 나타났습니다). So together is now correctly MESSAGE-driven for the flying supporter (arrival->True, departure->False), + RoomSeen confirm + the hold-봐 safety net. Reverted the wrong "combat drops 따라" change. All 9 suites green. Terminated everything; restarting fresh.

## Fixed: idle deadlock — 동전 (coins) counted as a MOB (marker mismatch)
User: waited >5m, not checking around, awake. ROOT CAUSE: hostiles() used _NON_MOB_MARKERS = (시체,핏자국,송장) — corpses only, NOT ground items. So a dropped 동전 ("동전 한 무더기가 놓여 있다") counted as a hostile -> area_barren `if s.hostiles(): return False` never fired -> stuck in clear_room forever; meanwhile _pick_target (which DOES skip 놓여/떨어져/오로라) never attacked it -> permanent idle (점수 watchdog + periodic 장비/방비), and the stale entities never refreshed so respawned mobs (confirmed live: 거대한 뱀×3 + 지네×3 present!) were never seen. The regen re-sweep never got a chance because area_barren gates the wait->watch->re-sweep cycle. FIX (access.py substrate): _NON_MOB_MARKERS now includes 놓여/떨어져/오로라 to MATCH hunting._ITEM_MARKERS, so hostiles()/area_barren/mobs_present agree with _pick_target. All 9 suites green. Fresh restart.
